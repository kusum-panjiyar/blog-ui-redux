// higher order component
// a component (function) which taakes another component as an argument or a component which returns another component as value is called as a higher order component

/* 
  higher order components == function
  connect - hoc 
  connect() -> return a  function
  connect()() -> here the returned function being invoked
  connect()(Count) -> returns another component (wrapped component)
*/